# ====================================
# Mission Control Configuration
# ====================================

# Database path (default: ./mission-control.db)
DATABASE_PATH=./mission-control.db

# ====================================
# OpenClaw Gateway Configuration
# ====================================

# OpenClaw Gateway URL (default: ws://127.0.0.1:18789)
# For remote connections via Tailscale: wss://your-machine.tail12345.ts.net
# For remote connections via Cloudflare: wss://gateway.yourdomain.com
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789

# OpenClaw Gateway authentication token (required for remote connections)
# Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# ====================================
# Remote Access Configuration (Choose One)
# ====================================
# For accessing Mission Control from outside your network
# Choose EITHER Tailscale OR Cloudflare Tunnel (not both)

# --- Option 1: Tailscale (Recommended for simplicity) ---
# Install Tailscale: https://tailscale.com/download
# On the Gateway machine: tailscale serve --bg 18789
# Then set OPENCLAW_GATEWAY_URL=wss://your-machine.tail12345.ts.net

# --- Option 2: Cloudflare Tunnel ---
# Install cloudflared: https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/
# Run: ./scripts/setup-cloudflare-tunnel.sh mission-control yourdomain.com
CLOUDFLARE_TUNNEL_TOKEN=
CLOUDFLARE_TUNNEL_CREDENTIALS_FILE=
CLOUDFLARE_TUNNEL_DOMAIN=

# ====================================
# API Keys (Required for Agent Tools)
# ====================================

# Brave Search API - Required for web_search tool
# Get your key: https://api-dashboard.search.brave.com/login
BRAVE_API_KEY=

# DeepSeek API - Optional alternative model provider
# Get your key: https://platform.deepseek.com/
DEEPSEEK_API_KEY=

# ====================================
# Workspace Configuration
# ====================================

# Mission Control API URL (auto-detected if not set)
# Override for remote access or custom deployments
# MISSION_CONTROL_URL=http://localhost:3000

# Base workspace directory (default: ~/Documents/Shared)
# All Mission Control files are stored here
# WORKSPACE_BASE_PATH=~/Documents/Shared

# Projects directory (default: ~/Documents/Shared/projects)
# Each project gets a unique folder here
# PROJECTS_PATH=~/Documents/Shared/projects

# ====================================
# Email Delivery Configuration (Optional)
# ====================================

# SMTP settings for sending deliverable emails
# FROM_EMAIL=your-sender@email.com
# FROM_NAME=Mission Control
# SMTP_SERVER=smtp.zoho.eu
# SMTP_PORT=465
# SMTP_PASSWORD=your-smtp-password

# Logo.dev API key for company logos in emails (optional)
# Get your key: https://logo.dev
# LOGO_DEV_KEY=your-logo-dev-key

# ====================================
# Security Notes
# ====================================
# 
# NEVER commit this file with real values!
# This is a template - copy to .env.local and configure:
#
#   cp .env.example .env.local
#
# Then edit .env.local with your actual values.
# .env.local is gitignored and will not be committed.
